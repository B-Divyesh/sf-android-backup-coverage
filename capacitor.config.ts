import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.sociobot.androidbackupcoverage',
  appName: 'Backup Coverage',
  webDir: 'dist',
  backgroundColor: '#F7F8F5',
  android: {
    backgroundColor: '#F7F8F5',
  },
};

export default config;
